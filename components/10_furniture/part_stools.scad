// =============================================================================
// TEA SHOP DIORAMA — GROUP 6: STOOLS (ALL 3 STOOLS)
// =============================================================================
// Standalone export for all 3 round wooden stools.
// Positioned directly in world coordinates on top of the base slab.
// =============================================================================

include <tea_shop_furniture_modules.scad>;

// 3 Stools clustered near counter window:
stool(index = 0, world_pos = true);
stool(index = 1, world_pos = true);
stool(index = 2, world_pos = true);
