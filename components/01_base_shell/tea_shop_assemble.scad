// =============================================================================
// tea_shop_assemble.scad — ASSEMBLED PREVIEW (visual verification only).
// Shows every Base & Shell piece together in its own colour. This file is NOT
// for export — use the part_*.scad files to export each piece as its own STL.
// =============================================================================

include <tea_shop_modules.scad>;

assemble();