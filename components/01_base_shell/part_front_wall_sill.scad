// =============================================================================
// part_front_wall_sill.scad — export the FRONT WALL SILL (knee-wall) only.
// Open this file, press F6 (render), then export as STL. Everything sits in the
// shared global coordinate system, so re-importing all parts together lines up
// automatically with no manual repositioning.
// =============================================================================

include <tea_shop_modules.scad>;

front_wall_sill();