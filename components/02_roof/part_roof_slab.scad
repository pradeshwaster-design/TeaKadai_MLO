// =============================================================================
// part_roof_slab.scad — export the STRUCTURAL ROOF SLAB only.
// Press F6 (render), then export as STL.
// Positioned in shared global coordinates to align automatically with Group 1.
// =============================================================================

include <tea_shop_roof_modules.scad>;

roof_slab();
